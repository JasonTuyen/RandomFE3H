import tkinter as *

window = Tk()

l1=label(window, text="Title")
l1.grid(row=0, column=0)

l1=label(window, text="Title")
l1.grid(row=0, column=0)

l1=label(window, text="Title")
l1.grid(row=0, column=0)

l1=label(window, text="Title")
l1.grid(row=0, column=0)

window.mainloop()
